"""
tests/fixtures
==============

Pytest fixtures exposing provider mocks for integration and unit tests.

All fixtures in this package are re-exported from ``conftest.py`` so they are
automatically discovered by pytest without any explicit import.
"""
